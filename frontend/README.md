Frontend (React + Vite)
=======================

Run from this folder:

1) Install dependencies
   `npm install`

2) Copy environment file
   `copy .env.example .env`

3) Start development server
   `npm run dev`

The app expects backend API at:
`http://localhost:8000/api/v1`
